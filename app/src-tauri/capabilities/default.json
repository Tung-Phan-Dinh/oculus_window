{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The app's own UI. Scoped to the main WEBVIEW, not the main window: the window also holds one webview of remote content per in-app browser tab (src/browser.rs), and window/webview scopes match by OR — a window scope would hand every Tauri command to whatever page the user browses to.",
  "webviews": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:webview:allow-set-webview-zoom",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "opener:default",
    "dialog:allow-open",
    "sql:allow-execute",
    "sql:allow-select",
    "sql:allow-load",
    "sql:allow-close"
  ]
}
